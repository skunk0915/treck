# API Configuration
API_KEY=your_api_key_here

# Base URL
# Only needed if using OpenAI models via a proxy.
# BASE_URL=https://your-custom-endpoint/v1

# Models
# For text generation (prompt creation)
# Use 'gpt-4o' for OpenAI, or 'gemini-1.5-flash' for Google
TEXT_MODEL=gemini-1.5-flash

# For image generation
# Use 'dall-e-3' for OpenAI, or 'google-nano-banana-pro' (or 'imagen-3.0-generate-001') for Google
IMAGE_MODEL=google-nano-banana-pro

# Application Settings
ARTICLE_DIR=article
IMG_DIR=img
CHAR_LIMIT=300
